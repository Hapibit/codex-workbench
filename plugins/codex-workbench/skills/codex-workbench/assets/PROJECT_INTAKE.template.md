# {{PROJECT_NAME}} 项目预处理画像

本文件用于在项目规划、工作台确认和功能开发前，把模糊需求转成可执行、可审查、可更新的项目画像。

使用规则：

- 本文件是项目级事实源，不是一次性需求聊天记录。
- 新需求改变用户、范围、权限、数据、AI 边界或验收时，先更新本文件，再更新 `PRODUCT_BASELINE.md`、`DEVELOPMENT_FLOW.md` 和相关功能包。
- 不能从聊天记录直接进入实现；影响交付的结论必须在本文件找到出处。
- 不确定但可以先做的内容写入“可默认假设”；会改变实现路线或验收的内容写入“阻塞问题”。

status: draft
owner: project owner
intake_updated_at: unconfirmed

## 一句话项目目标

- 这个项目要解决什么问题：
- 谁最需要它：
- 第一版成功的最低标准：
- 不做这个项目会出现什么问题：
- 第一版失败的明确表现：

## 用户和角色

主要用户：

-

管理员/运营/教师/审核等角色：

-

不服务的用户：

-

## 第一版范围

第一版必须有的 3 个能力：

- [ ]
- [ ]
- [ ]

第一版明确不做：

-

后续版本再考虑：

-

## 核心业务流程

主流程：

1.
2.
3.

失败/异常流程：

-

用户看得见的结果：

- 成功时用户看到：
- 失败时用户看到：
- 管理员/运营需要看到：

## 数据和权限

涉及的数据：

- 用户数据：
- 组织/租户数据：
- 学生/课程/成绩/订单/文件等敏感数据：
- AI 生成内容：

权限边界：

- 谁能看：
- 谁能改：
- 谁不能访问：

## AI 使用边界

如果涉及 AI：

- AI 负责什么：
- AI 不允许做什么：
- AI 输出是建议还是自动生效：
- 是否需要人工确认：
- 允许使用的数据来源：
- 日志和隐私要求：
- 评估通过标准：

## 技术和交付约束

- 必须使用的技术栈：
- 禁止使用的技术或服务：
- 目标部署环境：
- 第三方服务：
- 本地开发要求：
- 浏览器/移动端要求：

## 质量和验收

第一版可交付必须证明：

- [ ] 主流程可跑通。
- [ ] 至少一个关键失败路径有反馈。
- [ ] 权限和数据归属没有明显漏洞。
- [ ] AI 输出有边界和验证标准。
- [ ] 有可运行的构建、测试、lint、smoke 或手工验收记录。

验收证据要求：

- 证据位置：
- 可复现步骤：
- 期望结果：
- 实际结果：
- 未验证项和原因：

需求变更处理：

- 如果第一版范围变化，必须同步更新本文件和第一个功能 `SPEC.md`。
- 如果权限、数据或 AI 自动生效规则变化，必须重新评估 `FEATURE_WORKFLOW.md` 的工作量分级。
- 如果用户只提出一句模糊需求，先补阻塞问题，不要直接生成实现计划。

## 阻塞问题

这些问题不回答，不能把 `DEVELOPMENT_FLOW.md` 从 draft 改成 confirmed，也不应开始高风险功能实现。

| 编号 | 问题 | 为什么阻塞 | 答案 | 状态 |
| --- | --- | --- | --- | --- |
| P001 | 第一版最小可用范围是什么？ | 影响产品下限、功能拆分和验收标准。 |  | open |
| P002 | 主要用户和权限边界是什么？ | 影响数据归属、接口和 UI。 |  | open |
| P003 | 是否涉及 AI 自动生效或敏感数据？ | 影响人工确认、日志、隐私和 eval。 |  | open |

## 可默认假设

可以先按项目惯例处理，但要在后续确认。

| 编号 | 假设 | 依据 | 风险 | 是否需要确认 |
| --- | --- | --- | --- | --- |
| A001 |  |  |  |  |

## 生成下游文件前检查

- [ ] `PRODUCT_BASELINE.md` 已根据本文件更新。
- [ ] `DEVELOPMENT_FLOW.md` 的范围和确认条件已根据本文件更新。
- [ ] 第一个 `workbench/features/<feature-name>/SPEC.md` 已继承本文件里的第一版范围。
- [ ] 阻塞问题已经关闭，或明确说明为什么可以暂不关闭。
